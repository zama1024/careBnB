json.extract! listing, :id, :host_id, :title, :description, :daily_rate, :donation_percentage, :address, :city

# json.reviews do
#   json.partial! 'api/reviews/review', collection: bench.reviews, as: :review
# end
